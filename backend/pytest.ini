[pytest]

# Ensures the app module can be imported correctly
pythonpath = .

# Test discovery patterns
testpaths = tests

# Show extra summary info for skipped/failed tests
addopts = -ra

# Display warnings in test output
filterwarnings =
    ignore::DeprecationWarning